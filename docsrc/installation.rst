============
Installation
============

At the command line::

    $ easy_install mettoolbox

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv mettoolbox
    $ pip install mettoolbox
